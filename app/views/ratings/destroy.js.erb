var movie = $('#movie-<%= @movie.id %>');
movie.removeClass('checked');

var checkbox = movie.find('.checkbox');
checkbox.html("<%= j render('lists/rating', movie: @movie, rating: false) %>");

addPositionToMovies();

// Update progress bar
var progress = $('#progress');
progress.html("<%= j render('lists/progress') %>");
