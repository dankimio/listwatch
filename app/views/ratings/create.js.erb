var movie = $('#movie-<%= @movie.id %>');
movie.addClass('checked');

var checkbox = movie.find('.checkbox');
checkbox.html("<%= j render('lists/rating', movie: @movie, rating: true) %>");

addPositionToMovies();
